# Note: this is only compatible with Linux runners.

name: Test setup
inputs:
  pull_token:
    description: "Token to use for private repo access"
    required: true
  setup_gcc:
    description: "Whether to setup GCC or not"
    required: false
    default: 'true'
  setup_aws_cli:
    description: "Whether to install AWS CLI or not"
    required: false
    default: 'true'
runs:
  using: "composite"
  steps:
    - name: Set up git private repo access
      shell: bash
      run: |
        git config --global url."https://${{ inputs.pull_token }}@github.com/".insteadOf ssh://git@github.com
        git config --global url."https://${{ inputs.pull_token }}@github.com".insteadOf https://github.com

    - name: Install Go 1.22
      uses: actions/setup-go@v5
      with:
        go-version: "1.22"
        cache-dependency-path: "**/go.sum"

    - name: Print go version
      shell: bash
      run: go version

    - name: Check GCC version
      id: check-gcc
      shell: bash
      run: |
        if command -v gcc &> /dev/null; then
          echo "gcc_exists=true" >> $GITHUB_OUTPUT
          echo "gcc_version=$(gcc --version | head -n1 | awk '{print $NF}')" >> $GITHUB_OUTPUT
        else
          echo "gcc_exists=false" >> $GITHUB_OUTPUT
        fi

    - name: Setup GCC
      uses: Dup4/actions-setup-gcc@v1
      if: inputs.setup_gcc == 'true' && steps.check-gcc.outputs.gcc_exists != 'true'
      with:
        version: latest

    - uses: actions/setup-python@v5
      with:
        python-version: '3.10' 

    - name: Install AWS CLI v2
      if: inputs.setup_aws_cli == 'true'
      shell: bash
      run: |
        if ! command -v aws &> /dev/null; then
          echo "AWS CLI not found. Installing..."
          python3 -m pip install --user awscli
          echo "$HOME/.local/bin" >> $GITHUB_PATH
        else
          echo "AWS CLI is already installed."
        fi
        export PATH="$HOME/.local/bin:$PATH"
        aws --version

    - name: rust-cache
      uses: actions/cache@v3
      with:
        path: |
          ~/.cargo/bin/
          ~/.cargo/registry/index/
          ~/.cargo/registry/cache/
          ~/.cargo/git/db/
          target/
          ~/.rustup/
        key: rust-
        restore-keys: rust-

    - name: Setup toolchain
      id: rustc-toolchain
      shell: bash
      run: |
        curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- --default-toolchain 1.82.0 -y
        . "$HOME/.cargo/env" 
        echo "$HOME/.cargo/bin" >> $GITHUB_PATH

    - name: Install pkg-config, openssl, cmake, and unzip
      shell: bash
      run: |
        if ! dpkg -s pkg-config libssl-dev cmake unzip &> /dev/null; then
          echo "pkg-config, libssl-dev, cmake, and unzip not found. Installing..."
          sudo apt-get update
          sudo apt-get install -y pkg-config libssl-dev cmake unzip
        else
          echo "pkg-config, libssl-dev, cmake, and unzip are already installed."
        fi

    - name: Echo docker buildx version
      shell: bash
      run: docker buildx version

    - name: Install Protoc
      uses: arduino/setup-protoc@v3
      with:
        version: "29.4"

    - name: Set up Docker
      uses: crazy-max/ghaction-setup-docker@v3
      with: 
        version: 28.5.2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      with:
        driver-opts: |
          image=public.ecr.aws/vend/moby/buildkit:buildx-stable-1